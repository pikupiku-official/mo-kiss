import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "jp.mokiss.kseditor",
  appName: "mo-kiss KSエディタ",
  webDir: "www",
  server: {
    androidScheme: "https"
  }
};

export default config;
