{
  "name": "mo-kiss-ks-editor-android",
  "private": true,
  "version": "0.1.0",
  "description": "Android wrapper for the mo-kiss KS editor",
  "scripts": {
    "build:web": "powershell -NoProfile -ExecutionPolicy Bypass -File ./build-android.ps1",
    "cap:sync": "npx cap sync android",
    "android": "npm run build:web && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
